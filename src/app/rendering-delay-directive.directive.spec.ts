import { RenderingDelayDirective } from './rendering-delay-directive.directive';

describe('RenderingDelayDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderingDelayDirective();
    expect(directive).toBeTruthy();
  });
});
